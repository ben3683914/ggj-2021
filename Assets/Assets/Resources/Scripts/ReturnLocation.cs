using UnityEngine;
using System.Collections;

public class ReturnLocation : MonoBehaviour
{
    public ReturnLocations Location;

    public enum ReturnLocations
    {
        One,
        Two,
        Three
    }
}
